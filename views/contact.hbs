<!DOCTYPE html>
<html lang="en" dir="ltr">
   <h2>Contact me</h2>
<form action="/contact/send-message" method="post">
    {{#if isSent}}
  <div class="isSent">This message has been sent. File added: {{file}}</div>
    {{/if}}
    {{#if isError}}
      <div class="isError">You can't leave fields empty</div>
    {{/if}}
    {{#if isValid}}
      <div class="isValid">Wrong extension of a file</div>
    {{/if}}
  <label>
    Your name: <input type="text" name="author">
  </label>
  <label>
    Your email: <input type="email" name="sender">
  </label>
  <label>
    Title: <input type="text" name="title">
  </label>
  <label>
    Your file: <input accept=".png, .jpg, .jpeg, .gif" type="file" name="file">
  </label>
   <label>
    Message: <textarea name="message"></textarea>
  </label>
  <button type="submit">Send message</button>
</form>
</html>